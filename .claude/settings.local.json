{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write \"**/*.{html,css,js,json,md}\" --log-level silent 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write \"**/*.{html,css,js,json,md}\" --log-level silent 2>/dev/null; npx eslint --fix \"**/*.js\" 2>/dev/null; git add -A && (git diff --cached --quiet 2>/dev/null || git commit -m \"auto: checkpoint $(date '+%Y-%m-%d %H:%M')\" 2>/dev/null) || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
